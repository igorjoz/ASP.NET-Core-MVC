@{
    /* Use the shared layout for all Razor views so the <title> element is present.
       Individual views can set ViewData["Title"] to customize the page title. */
    Layout = "~/Views/Shared/_Layout.cshtml";
}
